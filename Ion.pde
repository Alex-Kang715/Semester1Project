public class Ion {
  String name, formula;
  int solCase, charge;
  public Ion(String name, String formula, int solCase, int charge) {
  name = name;
  formula = formula;
  solCase = solCase;
  charge = charge;
  }
}
